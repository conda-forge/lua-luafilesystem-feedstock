{% set name = "lua-luafilesystem" %}
{% set version = "1.8.0" %}
{% set sha256 = "16d17c788b8093f2047325343f5e9b74cccb1ea96001e45914a58bbae8932495" %}

package:
  name: {{ name }} 
  version: {{version }}

source:
  url: https://github.com/keplerproject/luafilesystem/archive/refs/tags/v1_8_0.tar.gz

build:
  number: 0
  skip: true  # [win]
  rpaths:
    - lib/lua/5.4/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - luarocks
    - libgcc  # [linux]
    - zlib
    - lua
  run:
    - luarocks
    - libgcc  # [linux]
    - zlib
    - lua    

test:
  commands:
    - lua -e "require 'lfs'"

about:
  home: 'https://keplerproject.github.io/luafilesystem/'
  license: MIT/X11
  license_family: MIT
  license_file: LICENSE 
  summary: 'File System Library for the Lua Programming Language'
  description: |
       LuaFileSystem is a Lua library developed to complement the set of 
       functions related to file systems offered by the standard Lua distribution.
       LuaFileSystem offers a portable way to access the underlying directory structure and file attributes.
       LuaFileSystem is free software and uses the same license as Lua 5.x (MIT).



extra:
  recipe-maintainers:
    - jerowe
    - izahn
